import SnsDetail from "@/features/SnsMain/SnsDetail";

/**
 * 작업자 : 진순흠
 * 작업 일시 : 2025.05.03
 * 작업 내용 : SNS디테일 페이지
 * @returns <SNsDetail />
 */

const SnsDetailPage = () => {
  return <SnsDetail />;
};

export default SnsDetailPage;
